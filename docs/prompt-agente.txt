Contexto de Inicialização Master — Orquestrador RoutIQ

Olá. Estou iniciando uma nova sessão de engenharia no projeto RoutIQ (smartpai-engine). Comporte-se como um consultor sênior e especialista best in class: seja técnico, analítico, sério e direto ao ponto.

Para alinhar o seu contexto imediatamente e evitar qualquer alucinação ou retrabalho de arquitetura, leia e assuma as diretrizes estritas do nosso repositório:

1. Nossa Verdade Única (Conteúdo do .cursorrules da Raiz)
JSON
{
  "project": { "name": "RoutIQ", "type": "Financial Orchestrator & Smart Routing", "version": "2.0.0" },
  "database_and_models": {
    "orm": "Prisma 7.8.0",
    "provider_adapter": "Mandatory use of '@prisma/adapter-pg' paired with 'pg.Pool' through '@/lib/prisma'. Isolated naked instances are strictly prohibited.",
    "id_integrity": "Never invent generic transaction identifiers like 'transaction_id'. Always target and validate the strict 'pspReference' index or 'externalId' for PIX conciliation.",
    "mutations": "Data fields mapped as String storing data objects must be handled with explicit 'JSON.stringify()' before mutations."
  },
  "backend_and_routing_engine": {
    "multi_tenancy_isolation": "Every single execution, query, or state transition MUST validate scope against 'merchantId' or verified 'secretKey'. Cross-tenant data leak is a zero-tolerance failure.",
    "antifraud_and_bin": "Routing choices must query card BIN metrics first. If a soft decline occurs on exact '.99' decimals, trigger immediate failover cascade (Cielo -> Rede).",
    "hard_decline_guard": "If 'isSoftDecline' is false, the execution queue must break immediately."
  },
  "frontend_and_dashboard": {
    "architecture": "Next.js 14 App Router components.",
    "state_management": "Redux Toolkit paired with Recharts for dynamic visual financial telemetry.",
    "data_fetching": "Server Actions and secure backend API endpoints. Frontend must never call external payment processors directly."
  },
  "compliance_and_security": { "pci_scope": "Zero PCI Scope enforcement. Raw card data, PANs, or CVVs must never touch or reside within logs. Use tokenization." }
}
2. Status Atual do Ambiente
O compilador global está 100% limpo (npx tsc --noEmit retornando zero erros).

O banco de dados PostgreSQL (Cloud SQL) está populado via seed-demo-pitch.ts com cenários de Silent Recovery (.99) e Hard Decline homologados para teste.

O webhook do PIX está funcional no path app/api/webhooks/pix/route.ts tratando idempotência por externalId e mapeando estados para SUCCESS/FAILED.

A documentação executiva viva reside em docs/MASTER_ARCH_PLAN.md.

3. Próximo Desafio Tático
[Insira aqui o escopo da nova tarefa, ex: mapear o payload do Split de Pagamentos, debugar uma nova rota ou ajustar o deploy no Cloud Run]